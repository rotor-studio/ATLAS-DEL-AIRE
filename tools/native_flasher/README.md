# Flasher nativo

Utilidad minima para Windows:

- detecta puertos serie reales del sistema
- usa un firmware fijo llamado `atlas-del-aire-latest.bin`
- solo deja elegir puerto y actualizar

El firmware se busca en este orden:

1. junto al ejecutable
2. `firmware/releases/atlas-del-aire-latest.bin`
